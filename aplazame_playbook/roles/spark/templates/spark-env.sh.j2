export SPARK_MASTER_HOST="{{ spark_masters[0]['ip'] }}"
export SPARK_LOCAL_DIRS="{{ spark_local_dir }}"
export SPARK_MASTER_WEBUI_PORT="{{ spark_master_webui_port }}"
export SPARK_WORKER_WEBUI_PORT="{{ spark_worker_webui_port }}"
export SPARK_WORKER_OPTS="-Dspark.worker.cleanup.enabled=true -Dspark.worker.cleanup.interval=900 -Dspark.worker.cleanup.appDataTtl=3600"
